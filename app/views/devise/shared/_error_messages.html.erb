<% if resource.errors.any? %>
  <div id="error_explanation" data-turbo-cache="false">
    <ul class="flex flex-col gap-1 p-0 text-sm italic text-red-400 list-none">
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
